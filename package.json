{
  "name": "boiler-api",
  "version": "0.1.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "test": "NODE_ENV=test jest",
    "start": "NODE_ENV=prod node -r ts-node/register src/app.ts",
    "dev": "NODE_ENV=dev nodemon --exec node -r ts-node/register src/app.ts",
    "live-dev": "NODE_ENV=prod nodemon --exec node -r ts-node/register src/app.ts",
    "syncDB-prod": "NODE_ENV=prod node -r ts-node/register src/database/sync.ts",
    "syncDB-dev": "NODE_ENV=dev node -r ts-node/register src/sync.db.ts",
    "start-docker": "docker build -t boiler-api . && docker run --env-file .env.dev -p 8001:8001 boiler-api"
  },
  "author": "Open Earth Labs",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/supertest": "^2.0.16",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "axios-rate-limit": "^1.3.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.1",
    "mysql2": "^3.5.2",
    "sequelize": "^6.32.1",
    "validatorjs": "^3.22.1"
  }
}
